import { createAppThemeProvider } from '@os/apps/utils/createAppThemeProvider';

import serviceRequestTheme from '../service_request.theme';

export const ServiceRequestThemeProvider = createAppThemeProvider(serviceRequestTheme);
